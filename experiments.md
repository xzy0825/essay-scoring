# 实验记录

数据：`train.csv` 15576 行，`test.csv` 1731 行。分数分布为 1:1124，2:4249，3:5629，4:3563，5:876，6:135。验证都是按分数分层的 5 折，种子 42。QWK 的类别固定为 1–6。

## TF-IDF + LightGBM

词 1–3 gram（最多 2 万维）加字符 2–5 gram（最多 1 万维），再加字符数、词数、句数、段落数、平均词长。LightGBM 回归，学习率 0.05，最多 3000 棵树，100 轮不提升就停。实际停在 269 到 442 棵。

四舍五入后的折外 QWK：

- fold 1：0.77710
- fold 2：0.77906
- fold 3：0.77134
- fold 4：0.77833
- fold 5：0.78351
- 总体：0.77786

用时 1912 秒。早停用了验证折，这个数略乐观。

## DeBERTa-v3-small

mean pooling 加线性头，MSE，最大长度 512，3 个 epoch，batch 4，梯度累积 4，学习率 2e-5，fp16 前向。每个折保留验证 QWK 最高的 epoch。作文 token 长度中位数 405，上限被截到 512。

四舍五入后的折外 QWK：

- fold 1：0.80028（第 3 个 epoch）
- fold 2：0.79206（第 2 个 epoch）
- fold 3：0.79788（第 2 个 epoch）
- fold 4：0.78961（第 3 个 epoch）
- fold 5：0.80390
- 总体：0.79678

用时 2836 秒。epoch 是按验证折挑的，和 LightGBM 的早停一样，折外分会略高。RTX 3080 上显存大约 4.3GB，没有降到 batch 2。

## 阈值和模型选择

在折外连续分数上搜索 5 个切分点，使 QWK 最大。切分点也是用这批折外样本定的，所以下面的阈值分数比四舍五入更乐观。

- LightGBM 四舍五入 0.77786，阈值 0.80329
- DeBERTa 四舍五入 0.79678，阈值 0.80902
- 两者平均 四舍五入 0.80223，阈值 0.82000

提交文件用平均模型加阈值。平均的切分点是 1.791、2.527、3.422、4.237、5.018。测试集预测分布为 1:127，2:478，3:619，4:353，5:140，6:14，和训练集一样集中在 2–4 分。

## optimize 分支

早停和选 epoch 改到训练折内部的 10%。DeBERTa 最大长度 1024，batch 2，梯度累积 8，回归头加上 log 篇幅。只有 112 篇作文顶到 1024。

四舍五入后的折外 QWK：

- LightGBM：0.77410、0.77804、0.77164、0.77827、0.77764，总体 0.77593。树的棵数是 231、267、309、328、162。
- DeBERTa：0.81521、0.81166、0.80846、0.80752、0.81790，总体 0.81216。用时 3724 秒。

第 0 折留出。校准折上按四舍五入 QWK 选择混合权重，DeBERTa 权重被选成 1.0，LightGBM 没有进提交。留出折四舍五入 0.81521，按题目切分后 0.82318。线上分数 **0.82084**，低于 main 的 **0.83237**。按题目设阈值把本地留出分抬高了，线上没有跟上。

## 全局阈值混合

同一套 small（1024）和 main 的 LightGBM，改回一套全局阈值，不再按题目切。small 权重大时本地阈值 QWK 更高，线上却是 LightGBM 占一半附近更好。

| 混合 | 线上 QWK |
|---|---|
| 0.80 small + 0.20 LightGBM | 0.835 |
| 0.60 small + 0.40 LightGBM | 0.83667 |
| 0.50 small + 0.50 LightGBM | 0.8367 |
| 0.40 small + 0.60 LightGBM | 0.83429 |
| 0.55 small + 0.45 LightGBM | 0.83298 |
| 0.65 small + 0.35 LightGBM | 0.8339 |

0.50 和 0.60 并列最好。再细一档会掉到 0.833 左右，测试集噪声大约在第三位小数。

在 DeBERTa 折外预测上再训一层 LightGBM（加上旧模型的预测、篇幅和题目）本地阈值 QWK 到了 0.831，线上 0.83521，没有超过 0.50 混合。各模型先标准化再相加，排序变差。

## DeBERTa-v3-base

最大长度 1024，batch 2，梯度累积 8，MSE，其余和 small 相同。五折四舍五入 QWK：0.81843、0.81148、0.81858、0.81562、0.80716，总体 **0.81421**。用时 7350 秒。顶到 1024 token 的仍是 112 篇。和 small 的折外预测相关系数 0.982。

单独换 base 几乎不动，和 small、LightGBM 一起混才拉开。线上最好的三模型比例是 0.45 base + 0.20 small + 0.35 LightGBM，**0.84568**。四周的权重在 0.840 到 0.845 之间，没有更高的。

## 加强 LightGBM

在 TF-IDF 和篇幅之外加上：词典外词比例（`/usr/share/dict/words`，并放进题目用词）、连续三个相同字母、问号和逗号、词种数、段落长度的均值和标准差、短段比例、连接词，以及题目 one-hot。早停仍在训练折内部的 10%，然后在整个训练折上重训。

四舍五入折外 QWK：0.78062、0.77758、0.77870、0.78723、0.78040，总体 **0.78092**。用时 3104 秒。和 main 那版 LightGBM 的相关系数是 0.986。

把它按 0.15 掺进原来的最优混合（base 0.45、small 0.20、旧 LightGBM 0.20）线上 **0.84594**，是目前最高分。用它完全替换旧 LightGBM（0.45 / 0.20 / 0.35）线上 0.84505，更低。

## 长文结尾

base 只在前 1024 token 上训练。对超过这个长度的作文，用同一套折权重再预测最后 1024 token，和开头的分数平均。训练集 112 篇，测试集 13 篇。

这 112 篇只看开头的折外 QWK 是 0.616。开头和结尾各一半是 0.604，只看结尾是 0.606。测试集那 13 篇的连续分数变了，但没有一篇跨过原来的切分点，整数提交和 0.84594 相同。

## 6 类分类头

base 的 MSE 换成 6 类交叉熵，预测用类别期望。类别权重是频数平方根的倒数，再归一化。同一次训练加上题目 one-hot，以及层间学习率衰减 0.9（顶层 2e-5，embedding 约 5.6e-6）。最大长度、batch 和 epoch 数不变。用时 7355 秒。

五折四舍五入 QWK：0.81869、0.80696、0.81531、0.80970、0.82217，总体 **0.81459**。1 分的平均预测从回归版的 1.648 收到 1.619，6 分从 5.342 掉到 5.098。和回归版 base 的相关系数是 0.980。

线上：0.25 分类 base + 0.20 回归 base + 0.20 small + 0.20 旧 LightGBM + 0.15 rich LightGBM 为 **0.84249**。0.45 分类 base + 0.20 small + 0.35 旧 LightGBM 为 **0.84113**。都低于 0.84594。

## 目前采用的提交

`src/submit.py` 固定为：

0.45 × base（MSE，1024）+ 0.20 × small（1024）+ 0.20 × main LightGBM + 0.15 × rich LightGBM

阈值在全部折外预测上搜索，一套全局切分点。线上 **0.84594**。

## 环境上碰到的问题

- huggingface.co 连接被重置，改用 `HF_ENDPOINT=https://hf-mirror.com`。
- DeBERTa 分词器需要 `protobuf`，已写入项目依赖。
- 官方 checkpoint 是 `pytorch_model.bin`。transformers 在 torch 2.5 上拒绝直接加载它，训练脚本会转成 safetensors 再读。
